{
    "version": "0.2.0",
    "configurations": [
        {
            "type": "chrome",
            "request": "launch",
            "name": "Iniciar Chrome para localhost",
            "url": "http://localhost:3000/api/products",
            "webRoot": "${workspaceFolder}"
        }
    ]
}